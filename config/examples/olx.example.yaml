# Placeholder: OLX Brasil — integrações documentadas (developers.olx.com.br)
# Copy to config/local/olx.yaml. Use somente o que os termos e a API permitem; não commitar tokens.
# Docs: https://developers.olx.com.br/

version: 1
source_id: olx

auth:
  # Depende do produto/fluxo; preencher conforme doc OLX
  access_token: ""
  client_id: ""
  client_secret: ""

endpoints:
  # base: ""  # ex.: domínio de API indicado na documentação para anúncios / autos
  # autos_metadata: ""  # marcas, modelos, etc. — ver doc JSON autos

# Filtro conceitual para anúncios de automóveis (categoria/região conforme API)
search_defaults: {}
  # state: "SP"
  # category: ""

request_budget:
  max_requests_per_run: null
  max_requests_per_minute: null

field_map: {}
ingestion: {}
