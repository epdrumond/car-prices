# Placeholder: iCarros — acesso via API (OAuth; credenciais fornecidas após contato com a plataforma)
# Copy to config/local/icarros.yaml and fill. Do not commit secrets.
# Docs: https://www.icarros.com.br/apidocs/ (e processo de credenciaiamento atual)

version: 1
source_id: icarros

auth:
  oauth:
    client_id: ""
    client_secret: ""
    access_token: ""
    refresh_token: ""
    token_url: ""
  # Preencher conforme documentação vigente (URLs podem mudar)

endpoints:
  # base_url: ""   # ex.: base da API fornecida pelo iCarros
  # search: ""
  # listing_detail: ""

request_budget:
  max_requests_per_run: null
  max_requests_per_minute: null
  note: "Cotas e regras conforme acordo de uso da API."

field_map: {}
ingestion: {}
