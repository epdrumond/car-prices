# Placeholder: Mercado Livre (Brasil) — listagem de veículos via API pública
# Copy to config/local/mercadolivre.yaml (or merge into a single local file) and fill in secrets.
# Docs: https://developers.mercadolivre.com.br/

version: 1
source_id: mercadolivre

auth:
  # Criar app em https://developers.mercadolivre.com.br/ (obter client_id, client_secret se OAuth)
  client_id: ""
  client_secret: ""
  # Access token (gerado via OAuth; renovar conforme doc). Não commitar.
  access_token: ""

endpoints:
  # Site ID Brasil: MLB. Ajuste conforme a doc atual.
  site_id: "MLB"
  # Ex.: busca de itens; confirmar path oficial na documentação
  # search_url: "https://api.mercadolibre.com/sites/MLB/search"
  # item_detail_url: "https://api.mercadolibre.com/items/{id}"

# Categoria de veículos: usar ID real da categoria (carros, etc.) da documentação
category_id: ""

request_budget:
  max_requests_per_minute: null
  max_requests_per_run: null
  note: "Respeitar limites oficiais da API e headers de rate limit retornados."

# Mapear chaves da resposta JSON da API -> campos canônicos do projeto
field_map: {}
  # exemplo conceitual (ajustar aos retornos reais):
  # id: source_listing_id
  # price: price_brl
  # available_quantity: ...

# Metadados para runs de ingestão
ingestion: {}
